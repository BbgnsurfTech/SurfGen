{
  "name": "@surfgen/cli",
  "version": "0.1.0",
  "description": "SurfGen command-line interface",
  "license": "Apache-2.0",
  "type": "module",
  "bin": { "surfgen": "./dist/main.js" },
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "lint": "eslint src test",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "commander": "^14.0.0"
  },
  "devDependencies": {
    "tsup": "catalog:",
    "vitest": "catalog:",
    "@vitest/coverage-v8": "catalog:"
  }
}
